{{- if .Values.kafkadrop.enabled }}
---
# Kafka Drop5 Service (Kafdrop)
apiVersion: v1
kind: Service
metadata:
  name: kafka-drop5
  namespace: {{ .Values.global.namespace }}
  labels:
    app: kafka-drop5
spec:
  type: {{ .Values.kafkadrop.service.type }}
  selector:
    app: kafka-drop5
  ports:
  - port: {{ .Values.kafkadrop.service.port }}
    targetPort: {{ .Values.kafkadrop.service.port }}
    {{- if eq .Values.kafkadrop.service.type "NodePort" }}
    nodePort: {{ .Values.kafkadrop.service.nodePort }}
    {{- end }}
    name: http
---
# Kafka Drop5 Deployment (Kafdrop)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-drop5
  namespace: {{ .Values.global.namespace }}
  labels:
    app: kafka-drop5
spec:
  replicas: {{ .Values.kafkadrop.replicaCount }}
  selector:
    matchLabels:
      app: kafka-drop5
  template:
    metadata:
      labels:
        app: kafka-drop5
    spec:
      containers:
      - name: kafka-drop5
        image: {{ .Values.kafkadrop.image.repository }}:{{ .Values.kafkadrop.image.tag }}
        imagePullPolicy: {{ .Values.kafkadrop.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.kafkadrop.service.port }}
          name: http
        env:
        - name: KAFKA_BROKERCONNECT
          value: {{ .Values.kafkadrop.kafka.brokerConnect }}
        - name: JVM_OPTS
          value: {{ .Values.kafkadrop.jvmOpts }}
        - name: SERVER_SERVLET_CONTEXTPATH
          value: {{ .Values.kafkadrop.serverServletContextPath }}
        resources:
          {{- toYaml .Values.kafkadrop.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /
            port: {{ .Values.kafkadrop.service.port }}
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: {{ .Values.kafkadrop.service.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
{{- end }}
